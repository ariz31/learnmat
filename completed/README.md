# Completed task history

This directory preserves closed task records for future duplicate detection and
repository history.

Allowed statuses are `completed`, `superseded`, and `cancelled`. Never reuse or
delete a task ID simply because its implementation merged or was abandoned.

Search this directory before proposing work that may already have been solved.

See `docs/AGENT-TASK-LIFECYCLE.md`.
