# Package handoff

Purpose: documentation-first LearnMat repository starter plus the five supplied
surveying HTML sources. Copy compatible files into the target repository after
comparing existing files; do not overwrite active instructions blindly.

Run `python scripts/validate_catalog.py` from the extracted learnmat directory.
This checks structure, paths, and source integrity. It does not certify the HTML.

No live GitHub repository was inspected or modified. No website was built or
deployed. No GitHub Actions workflow is included. License selection and actual
asset reviews remain documented release gates.
